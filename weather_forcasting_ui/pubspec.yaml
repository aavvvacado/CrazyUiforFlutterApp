name: ui
description: "A new Flutter project."

# Prevents accidental publishing to pub.dev
publish_to: 'none'

# Defines the version and build number for the application
version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

# Dependencies required for the project
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  slider_button: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Ensure correct indentation and spacing for assets
  assets:
    - assets/images/
